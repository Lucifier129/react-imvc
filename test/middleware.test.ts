import cacheView from '../middleware/cacheView'
import createCache from '../middleware/createCache'
import shareRoot from '../middleware/shareRoot'

describe('middleware', () => {
  describe('cacheView', () => {
    
  })
  
  describe('createCache', () => {
    
  })
  
  describe('shareRoot', () => {
    
  })
})